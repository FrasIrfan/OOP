#include <iostream>
#include <iomanip>
using namespace std;

int main()
{
    int a;
    cout << setw(10) << "Number"; //sets the width of the field for the output of values in formatted output streams.
    cout << setw(10) << "Square";
    cout << setw(10) << "Cube";

    return 0;
}